import { styled } from "styled-components";
import { media } from "../../../styles/helpers";

export const Form = styled.form`
  display: grid;
  gap: 1.6rem;

  & > button {
    margin-top: 0.8rem;
  }
`;
